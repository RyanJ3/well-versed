export interface User {
  id: string;        // Unique identifier for the user
  email: string;     // User's email address
  name: string;      // User's full name
  denomination: string; // User's religious denomination
}
